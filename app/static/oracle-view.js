(()=>{
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[m]));
const quality={high:'Высокое',medium:'Среднее',low:'Низкое'},outcome={home:'Победа хозяев',draw:'Ничья',away:'Победа гостей'};
const list=(title,items=[])=>items.length?`<div style="margin:16px 0 6px;font-weight:850">${title}</div><div class="card" style="padding:10px 13px">${items.map(x=>`<div style="padding:7px 0;border-bottom:1px solid #172b40;font-size:13px">• ${esc(x)}</div>`).join('')}</div>`:'';
const matchRows=rows=>(rows||[]).map(x=>`<div style="padding:8px 0;border-bottom:1px solid #172b40;font-size:12px"><div style="display:grid;grid-template-columns:70px minmax(0,1fr) auto;gap:7px"><span style="color:#7f96aa">${esc(x.date||'')}</span><span>${esc(x.home||'')} — ${esc(x.away||'')}</span><b>${esc(x.score||'')}</b></div>${x.competition?`<div style="padding-left:77px;color:#667f95;font-size:10px;margin-top:3px">${esc(x.competition)}</div>`:''}</div>`).join('');
window.openOracle=async function(id){const modal=document.getElementById('modal'),box=document.getElementById('sheetContent'),m=GTS.match(id);box.innerHTML='<div class="loading">Оракул открывает сохранённый прогноз…</div>';modal.classList.add('open');try{const d=await GTS.api(`/api/oracle/matches/${id}`),p=d.probabilities||null,recent=d.recent_matches||{},h2h=d.head_to_head_matches||[];box.innerHTML=`<div class="sheet-round">✦ ОРАКУЛ · ${quality[d.data_quality]||esc(d.data_quality||'')}</div><div class="sheet-title" style="text-align:center">Прогноз ИИ</div>${m?`<div style="text-align:center;color:#93a8bd;font-size:13px">${esc(m.home.name)} — ${esc(m.away.name)}</div>`:''}<div style="text-align:center;font-size:42px;font-weight:950;margin:15px 0 2px">${d.home_score}:${d.away_score}</div><div style="text-align:center;color:#70e5ae;font-weight:850">${outcome[d.outcome]||esc(d.outcome||'')}</div><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:18px 0"><div class="card" style="padding:12px;text-align:center"><div class="meta">Уверенность</div><strong style="font-size:20px">${Number(d.confidence||0)}%</strong></div><div class="card" style="padding:12px;text-align:center"><div class="meta">Качество данных</div><strong>${quality[d.data_quality]||esc(d.data_quality||'')}</strong></div></div>${p?`<div class="card" style="padding:13px"><div class="meta">Вероятности 1 / X / 2</div><div style="display:flex;justify-content:space-between;margin-top:7px;font-weight:850"><span>${p.home}%</span><span>${p.draw}%</span><span>${p.away}%</span></div></div>`:''}<div style="margin:16px 0 6px;font-weight:850">Почему такой прогноз</div><div style="font-size:13px;color:#b8c9d8;line-height:1.5">${esc(d.reasoning||'')}</div>${recent.home?.length?`<div style="margin:16px 0 6px;font-weight:850">Последние матчи хозяев</div><div class="card" style="padding:0 12px">${matchRows(recent.home)}</div>`:''}${recent.away?.length?`<div style="margin:16px 0 6px;font-weight:850">Последние матчи гостей</div><div class="card" style="padding:0 12px">${matchRows(recent.away)}</div>`:''}${d.head_to_head?`<div style="margin:16px 0 6px;font-weight:850">История встреч</div><div style="font-size:13px;color:#b8c9d8;line-height:1.5">${esc(d.head_to_head)}</div>`:''}${h2h.length?`<div class="card" style="padding:0 12px;margin-top:8px">${matchRows(h2h)}</div>`:''}${list('Травмы и дисквалификации',d.injuries)}${list('Ключевые факторы',d.key_factors)}${list('Что может сломать прогноз',d.failure_risks)}<button class="close" onclick="closeSheet()">Закрыть</button>`}catch(e){box.innerHTML=`<div class="sheet-title">Оракул недоступен</div><div class="error">${esc(e.message)}</div><button class="close" onclick="closeSheet()">Закрыть</button>`}};
})();