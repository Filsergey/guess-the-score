(()=>{
const css=document.createElement('style');css.textContent=`
.match-participant{display:flex;align-items:center;gap:10px;padding:12px 0;border-bottom:1px solid #172b40}.match-participant:last-child{border-bottom:0}.match-participant .avatar{width:38px;height:38px;flex:0 0 38px}.match-participant-main{min-width:0;flex:1}.match-participant-name{font-weight:800;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.match-participant-pred{font-size:17px;font-weight:900;min-width:58px;text-align:right}.match-participant-pred.hidden{font-size:12px;color:#70e5ae;font-weight:750}.match-participant-pred.none{font-size:12px;color:#71869a;font-weight:700}.match-participant-points{font-size:11px;color:#70e5ae;text-align:right;margin-top:2px}.match-participant-points.zero{color:#71869a}.match-participant.oracle{background:rgba(106,90,255,.06);margin:0 -8px;padding-left:8px;padding-right:8px;border-radius:12px}.privacy-note{padding:10px 12px;border-radius:13px;background:#0d2134;border:1px solid #1e405e;color:#91a9bf;font-size:12px;line-height:1.35;margin:10px 0}.live-pred-head{display:flex;justify-content:space-between;align-items:center;padding:10px 12px;border-radius:13px;background:rgba(255,77,98,.08);border:1px solid rgba(255,77,98,.28);margin:10px 0}.live-pred-head strong{color:#ff7383}.live-pred-score{font-size:20px;font-weight:900}.match-rank{width:22px;height:22px;border-radius:50%;display:grid;place-items:center;font-size:10px;font-weight:900;color:#8ca4b9;background:#10263b;border:1px solid #244660;flex:0 0 22px}.match-rank.top{color:#07140f;background:#70e5ae;border-color:#70e5ae}.match-leaders{padding:9px 11px;border-radius:12px;background:#0c2a22;border:1px solid #235d49;color:#bff7df;font-size:11px;font-weight:800;margin-bottom:9px}.live-auto{text-align:center;font-size:9px;color:#6e879d;margin-top:-4px;margin-bottom:8px}
`;document.head.appendChild(css);
const e=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const matchById=id=>window.GTS?.match(id)||null;let activeId=null,timer=null,loading=false;
function stop(){if(timer){clearTimeout(timer);timer=null}}
function avatar(x){return x.avatar_url?`<img class="avatar" src="${e(x.avatar_url)}" alt="">`:`<div class="avatar">${x.is_oracle?'✦':''}</div>`}
function points(p,d){if(d.live&&p?.live_points!=null)return `<div class="match-participant-points ${p.live_points?'':'zero'}">+${p.live_points} сейчас</div>`;if(d.final&&p?.points!=null)return `<div class="match-participant-points ${p.points?'':'zero'}">+${p.points} очк.</div>`;return ''}
function rank(x,d){if(!(d.live||d.final)||x.match_rank==null)return '<span class="match-rank">—</span>';return `<span class="match-rank ${x.match_rank===1?'top':''}">${x.match_rank}</span>`}
function row(x,d){const p=x.prediction;let score,cls='';if(p)score=`${p.home_score}:${p.away_score}`;else if(x.has_prediction){score='✓ прогноз есть';cls='hidden'}else{score='Нет прогноза';cls='none'}return `<div class="match-participant ${x.is_oracle?'oracle':''}">${rank(x,d)}${avatar(x)}<div class="match-participant-main"><div class="match-participant-name">${e(x.display_name||x.username||'Игрок')}${x.is_mine?' · ты':''}</div><div class="meta">${x.is_oracle?'ИИ-соперник':x.username?'@'+e(x.username):x.member_role==='owner'?'владелец':'участник'}</div></div><div><div class="match-participant-pred ${cls}">${score}</div>${points(p,d)}</div></div>`}
function leaderBlock(d){if(!d.live||!d.leaders?.length)return '';const names=d.leaders.map(x=>x.is_oracle?'✦ Оракул':e(x.display_name)).join(', '),pts=d.leaders[0]?.live_points??0;return `<div class="match-leaders">Сейчас лидирует: ${names} · +${pts}</div>`}
function schedule(id,d){stop();if(d.live)timer=setTimeout(()=>refresh(id,true),30000)}
async function refresh(id,soft=false){if(loading||activeId!==id)return;loading=true;const modal=document.getElementById('modal'),box=document.getElementById('sheetContent'),m=matchById(id),leagueId=window.GTS?.leagueId;if(!modal?.classList.contains('open')){loading=false;return stop()}if(!soft)box.innerHTML='<div class="loading">Загружаем прогнозы…</div>';
 try{const qs=leagueId?`?league_id=${encodeURIComponent(leagueId)}`:'',d=await window.GTS.api(`/api/predictions/matches/${id}/participants${qs}`);if(activeId!==id)return;const rows=d.response||[],list=rows.map(x=>row(x,d)).join('');let note=d.predictions_visible?'Матч начался — прогнозы участников открыты.':'До начала матча чужие счета скрыты. Видно только, кто сделал прогноз.';if(d.live)note='Матч идёт. Временные очки считаются по текущему счёту и не попадают в официальную таблицу до финального свистка.';const live=d.live&&d.score?`<div class="live-pred-head"><strong>● LIVE · если закончится сейчас</strong><span class="live-pred-score">${d.score.home??0}:${d.score.away??0}</span></div>`:'';box.innerHTML=`<div data-participants-match-id="${id}"><div class="sheet-round">${e(m?.round||'Лига чемпионов')}</div><div class="sheet-title">Участники матча</div>${m?`<div style="text-align:center;color:#b7c9d9;font-weight:800;margin-bottom:10px">${e(m.home.name)} — ${e(m.away.name)}</div>`:''}${live}${leaderBlock(d)}<div class="privacy-note">${note}</div>${d.live?'<div class="live-auto">Автообновление каждые 30 секунд</div>':''}<div class="meta" style="margin:10px 0">Прогноз сделали: ${d.submitted_count??0} из ${d.human_member_count??0}${d.oracle_included?' · + Оракул':''}</div>${list||'<div class="empty">В лиге пока нет участников</div>'}<button class="close" onclick="closeSheet()">Закрыть</button></div>`;schedule(id,d)}catch(err){if(!soft)box.innerHTML=`<div class="sheet-title">Не удалось загрузить прогнозы</div><div class="error">${e(err.message)}</div><button class="close" onclick="closeSheet()">Закрыть</button>`}finally{loading=false}}
window.openMatchParticipants=function(id){activeId=Number(id);stop();return refresh(activeId,false)};
document.addEventListener('gts:goal',ev=>{const id=Number(ev.detail?.match?.id);if(activeId===id&&document.querySelector(`[data-participants-match-id="${id}"]`))refresh(id,true)});
document.addEventListener('gts:match-finished',ev=>{const id=Number(ev.detail?.match?.id);if(activeId===id&&document.querySelector(`[data-participants-match-id="${id}"]`))refresh(id,true)});
document.getElementById('modal')?.addEventListener('click',ev=>{if(ev.target===ev.currentTarget){activeId=null;stop()}});
})();